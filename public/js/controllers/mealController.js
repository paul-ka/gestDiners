/*function mealController($scope, mealService, guestService, dishService){

	alert($scope.name);

		while ($scope.name == true){
			
			$scope.trusty = 0;
			$scope.spIng = dish.ingredients.split(" ");
			$scope.spAll = guest.allergy.split(" ");
			$scope.spHat = guest.hated.split(" ");
			for (trusty = 0; trusty < 1; trusty ++){
				if ((spIng == spAll) || (spIng == spHat)){
					trusty += 1;
				}
			};
			if (trusty > 0){
				value: true;
			};
		};
	}
*/